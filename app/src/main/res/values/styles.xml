<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="EmptyMainImageButtonStyle">
        <item name="android:layout_width">@dimen/empty_main_image_width</item>
        <item name="android:layout_height">@dimen/empty_main_image_height</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:layout_marginTop">@dimen/empty_main_image_margin_top</item>
        <item name="android:background">@drawable/empty_recipe</item>
    </style>

    <style name="EmptyMainTitleStyle">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:text">@string/app_name</item>
        <item name="android:textColor">@color/empty_main_title_color</item>
        <item name="android:textSize">@dimen/empty_main_title_text_size</item>
    </style>

    <style name="EmptyMainNoConnectionStyle">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:text">@string/no_connection_text</item>
        <item name="android:gravity">center</item>
        <item name="android:layout_marginLeft">@dimen/no_connection_margin_left</item>
        <item name="android:layout_marginRight">@dimen/no_connection_margin_right</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
