<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.node_coyote.bakerscorner">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name=".recipeData.RecipeProvider"
            android:authorities="com.node_coyote.bakerscorner.recipeData"
            android:enabled="true"
            android:exported="false" />
        <provider
            android:name=".ingredientData.IngredientProvider"
            android:authorities="com.node_coyote.bakerscorner.ingredientData"
            android:enabled="true"
            android:exported="false" />
        <provider
            android:name=".stepData.StepProvider"
            android:authorities="com.node_coyote.bakerscorner.stepData"
            android:enabled="true"
            android:exported="false" />

        <activity android:name=".RecipeDetailActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
    </application>

</manifest>